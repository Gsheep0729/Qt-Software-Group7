# === GitHub 仓库安全过滤网 (单仓库多项目专用) ===

# --- 1. 保留所有 Git 元数据文件（根+子项目） ---
# 目的：让根仓库统一跟踪所有子项目的 Git 配置文件，避免子模块识别（子项目内不要手动建.git目录）
# 注意：根目录的.git是本地仓库核心，Git 天然不跟踪，无需配置
!**/.gitignore           # 不忽略任何目录下的.gitignore（根+所有子项目）
!**/.gitattributes       # 不忽略任何目录下的.gitattributes
!**/.gitmodules          # 不忽略任何目录下的.gitmodules
# 移除对**/.git/的忽略：但子项目内不要手动创建.git目录，否则Git仍会识别为子模块

# --- 2. 彻底禁止所有形式的构建目录 ---
**/build/
**/build*/
**/CMakeFiles/
**/_autogen/
**/Testing/
**/.qtcreator/

# --- 3. 彻底禁止二进制文件和库 ---
**/*.o
**/*.obj
**/*.a
**/*.lib
**/*.so*
**/*.dylib
**/*.dll
**/*.exe
**/*.pdb
**/*.ilk
**/*.exp
**/*.app

# --- 4. 彻底禁止 Qt 自动生成的代码 ---
**/ui_*.h
**/moc_*.cpp
**/moc_*.h
**/qrc_*.cpp
**/*.moc

# --- 5. 彻底禁止 CMake 生成的本地状态 ---
**/CMakeCache.txt
**/CMakeScripts/
**/cmake_install.cmake
**/install_manifest.txt
**/*.ninja
**/*.cmake

# --- 6. 彻底禁止 IDE 本地配置文件 (包含绝对路径) ---
**/*.user
**/*.user.*
**/.vscode/
**/.idea/

# --- 7. 操作系统杂质 ---
**/.DS_Store
**/Thumbs.db
**/.*.swp
**/*~
